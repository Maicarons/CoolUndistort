[package]
name = "coolundistort-infer"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-or-later"
description = "Rust inference core: calibration + TPS + rotation + prompts + ONNX hook"

[dependencies]
image = "0.25"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
tract-onnx = "0.23"
